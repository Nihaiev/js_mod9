/**
 * Створення проміса
 * класс promise
 * resolve
 * reject
 * Promise.prototype.then(onResolve, onReject)
 */

// const promise = new Promise((resolve, reject) => {
//     const canFulfill = Math.random() > 0.5;
//     setTimeout(() => {
//             if (canFulfill) {
//         resolve ('Проміс виконався успішно, з результатом')
//     }

//     reject('Проміс виконався з помилкою');
//     }, 2000)

// });

// promise.then(
//     result => {
//     console.log(`${result}`);
// },
//     error => {
//     console.log(`${error}`);
// })

/**
 * new Promise створює обіцянку яка записується у змінну promise
 * обіцянка це просто обєкт
 */

//30

/**
 * Промісифікація функцій 
 *  - Promise.resolve()
 *  - Promise.reject()
 */

